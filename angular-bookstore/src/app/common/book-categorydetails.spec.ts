import { BookCategorydetails } from './book-categorydetails';

describe('BookCategorydetails', () => {
  it('should create an instance', () => {
    expect(new BookCategorydetails()).toBeTruthy();
  });
});
